import logo from ".././logo.svg";

export const Header = () => {
    return (
        <header className="app-header item">
        <img src={logo} className="App-logo" alt="logo" />
      </header>
    )
}